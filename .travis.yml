os:
  - linux

language: bash

addons:
  apt:
    sources:
      - debian-sid
    packages:
      - shellcheck

before_script:
  - chmod +x ./hack/make.sh

script:
  - ./hack/make.sh test

notifications:
  email:
    on_success: never
